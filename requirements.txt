# ───────────────────────────────────────────────────────────────────────
# SENTINEL-AI Commander :: Python dependencies
# ───────────────────────────────────────────────────────────────────────
# Pinned for reproducibility. Regenerate with:
#   pip install -r requirements.txt
#   pip freeze > requirements.lock.txt   (if you want a fully-frozen snapshot)
# ───────────────────────────────────────────────────────────────────────

# ─── Web API / async runtime (ai_agents/) ──────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
dspy-ai>=2.4.0
groq>=0.4.0

# ─── Storage / DB ──────────────────────────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
redis>=5.0.0

# ─── HTTP / async clients ──────────────────────────────────────────────
httpx>=0.27.0
aiohttp>=3.9.0
aiofiles>=23.2.0

# ─── Wazuh indexer (OpenSearch fork) ───────────────────────────────────
opensearch-py>=2.4.0

# ─── Config / runtime ──────────────────────────────────────────────────
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
tenacity>=8.2.0
structlog>=24.1.0
requests>=2.31.0
urllib3>=2.0.0

# ─── ML/DL (Point 4: future anomaly detection) ─────────────────────────
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0

# ─── Ansible + Windows/MySQL automation ────────────────────────────────
# ansible (full) bundles all official collections; we pin the major series.
ansible>=9.0,<12.0
# ansible-core is pulled by ansible package, declared here for clarity.
ansible-core>=2.16,<2.18

# WinRM transport for Windows hosts (srv-ad-dns, srv-ftp)
pywinrm>=0.5.0

# MySQL driver used by community.mysql ansible collection
PyMySQL>=1.1.0
# Optional but recommended for community.mysql modules:
# cryptography is already a sub-dep of pywinrm but pinned here for clarity
cryptography>=41.0.0

# Used by jq-style queries inside ansible playbooks (common Ansible idiom)
jmespath>=1.0.1
